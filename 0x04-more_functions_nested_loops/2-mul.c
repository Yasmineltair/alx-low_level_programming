#include "main.h"

/**
  * mul - function that multiplies two integers
  * @a: the first number that will be multiplied
  * @b: the second number that will be multiplied
  * Return: return result
  */

int mul(int a, int b)
{
return (a * b);
}
